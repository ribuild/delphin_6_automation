language: python
python: 3.6
#dist: xenial
#sudo: required

install:
  - pip install -r requirements.txt

script:
  #- python -m pytest --cov-config .coveragerc --cov=delphin_6_automation pytest/
  - python -m pytest pytest/test_general_interactions.py::test_download_full_project_from_database

services:
  - mongodb

before_script:
  - mongo mydb_test --eval 'db.createUser({user:"travis",pwd:"test",roles:["readWrite"]});'

after_success:
  - coverage report
  - coveralls