<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>
<match *.**>
  @type stdout
  <store>
    @type stdout
  </store>
<match *.**>
  @type file
  path /fluentd/log
  compress gzip
  <buffer>
    timekey 1m
    timekey_use_utc true
    timekey_wait 10s
  </buffer>
</match>